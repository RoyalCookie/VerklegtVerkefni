#ifndef HELPER_H
#define HELPER_H
#include <iostream>
#include <fstream>
#include <vector>
#include "person.h"
#include <string>


class helper{

using namespace std;
class helper{
public:
    vector<person> Sort(vector<person> listi, char type);
    vector<person> Read();
    vector<person> Write(vector<person> listi);
};



